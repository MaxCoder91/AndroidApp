<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.androidapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".AgEventos"></activity>
        <activity android:name=".EditarEvento" />
        <activity android:name=".RestablecerPass" />
        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MiCalendario" />
        <activity android:name=".AgOtros" />
        <activity android:name=".AgEstudios" />
        <activity android:name=".AgTrabjo" />
        <activity android:name=".Eventos" />
        <activity android:name=".Registro" />
        <activity android:name=".MenuPrincipal" />
        <activity android:name=".MainActivity" />
    </application>

</manifest>